jtml 2
let pageTitle = "Dynamic Example"
let username = "Alice"
get welcomeMsg = ("Welcome, " + username + "!")

html
  head
    title
      show pageTitle
  body
    show "Title: " + pageTitle
    show welcomeMsg
    // unbind username
    show "After unbinding: " + username
